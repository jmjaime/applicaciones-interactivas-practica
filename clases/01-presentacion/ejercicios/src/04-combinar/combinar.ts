type Persona = { nombre: string; edad: number };

export function actualizarEdad(persona: Persona, nuevaEdad: number): Persona {
  // TODO: completar (ver README.md) — pista: spread
  return persona;
}
