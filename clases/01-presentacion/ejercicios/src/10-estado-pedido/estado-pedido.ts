// TODO: definir los valores del enum (ver README.md)
export enum EstadoPedido {}

export function siguienteEstado(estado: EstadoPedido): EstadoPedido {
  // TODO: completar (ver README.md) — pista: switch
  return estado;
}
