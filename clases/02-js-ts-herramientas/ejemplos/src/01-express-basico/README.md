# 1. Express básico

Primer servidor Express: routing con parámetros de ruta (`:nombre`) y
`res.json(...)`. Corresponde al type-along del Bloque 1 de
`../../../slides/clase2-slides.html`.

```bash
npm run dev
# GET http://localhost:3000/saludo/Ada
```
