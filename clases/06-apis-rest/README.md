# Clase 6 — APIs REST

Ejercicio único: ABM de `Propiedad` (dominio del TPO, marketplace inmobiliario) persistido con TypeORM, punta a punta — migraciones de esquema, seed, repository, service, controller con validación, manejo de errores centralizado y paginación. Capas Controller → Service → Repository, cada una en su propia carpeta.

## Ejercicios

Ver su [README](ejercicios/README.md).

```bash
cd ejercicios
npm install
npm test
```
