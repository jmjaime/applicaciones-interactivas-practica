/*
Enunciado
- Implementar `factorial(n: number): number` para `n` entero no negativo.

Pistas (JS/TS)
- Validá que `n` sea entero (`Number.isInteger(n)`) y `>= 0`.
- Para `n = 0`, el resultado es `1` por definición.
- Usá un bucle `for` multiplicando de 2 hasta `n`.

Referencias
- MDN - Number.isInteger: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number/isInteger
- MDN - for: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/for
*/
export function factorial(n: number): number {
  throw new Error("Not implemented");
}
