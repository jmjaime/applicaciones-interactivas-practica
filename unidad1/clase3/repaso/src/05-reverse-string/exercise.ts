/*
Enunciado: invertir un string

Objetivo
- Implementar una función `reverseString(text: string): string` que retorne el
  texto invertido. Ejemplo: "hola" -> "aloh".

Restricciones
- Usar TypeScript y mantener código simple y legible.
- No usar `Array.prototype.reverse()`. Podés iterar el string y construir el
  resultado.

Cobertura de tests sugerida (crear en `src/05-reverse-string/exercise.spec.ts`)
- Casos simples: "hola" -> "aloh", "abc" -> "cba".
- Cadena vacía: "" -> "".
- Con espacios y signos: "a b!" -> "!b a".

Tareas del alumno
1. Implementar la función debajo.
2. Escribir los tests en `exercise.spec.ts`.
3. Agregar un script npm en package.json para ejecutar sólo estos tests
   (por ejemplo: "reverse-string").

Referencias
- MDN - String: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String
- MDN - for...of: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/for...of
*/

// TODO: Implementar
export function reverseString(_text: string): string {
  throw new Error("Not implemented");
}
