/*
Enunciado
- Implementar `mostFrequentChar(text: string): string` que devuelva el carácter
  que más veces aparece en `text`. Si hay empate, devolver el que aparece
  primero al leer de izquierda a derecha. Distingue mayúsculas/minúsculas.

Pistas
- Usá un objeto (mapa) para contar apariciones por carácter.
- Hacé un segundo recorrido para resolver empates siguiendo el orden del texto.
- Si `text` es vacío, devolver `""`.

Referencias
- MDN - Object (uso como mapa simple): https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object
- MDN - String: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String
*/
export function mostFrequentChar(_text: string): string {
  throw new Error("Not implemented");
}
