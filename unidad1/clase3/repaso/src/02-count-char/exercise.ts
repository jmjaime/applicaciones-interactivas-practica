/*
Enunciado
- Implementar `countChar(text: string, target: string): number` que cuente
  cuántas veces aparece `target` dentro de `text`.

Pistas (JS/TS)
- Validá que `target` sea UN solo carácter usando `target.length === 1`.
- Recorre el string con `for...of` para obtener cada carácter.
- Compará con `===` y sumá en un contador.
- `text.length` te da la longitud total del string (por si lo necesitás).

Referencias
- MDN - String: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String
- MDN - for...of: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/for...of
*/
export function countChar(text: string, target: string): number {
  throw new Error("Not implemented");
}
